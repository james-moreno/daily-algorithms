// Print integers from -52 to 1066 using a for loop.

var testMin = -52;
var testMax = 1066;

function printRange(min, max) {
  for (var i = min; i <= max; i++) {
    console.log(i);
  }
}

printRange(testMin, testMax);
